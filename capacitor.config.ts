import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theshoecleaninghub',
  appName: 'The Shoe Cleaning Hub',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
